// import { fromJS } from 'immutable';
// import { makeSelectUsersListDomain } from '../selectors';

// const selector = makeSelectUsersListDomain();

describe('makeSelectUsersListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
